package ${context.packageName};

${context.imports}

#parse("comment.vm")

public interface ${context.entityName} #if(${context.hasInterfaces}==true) extends ${contex.interfaces} #end{
    #if(!${context.isMutable})
        static final int PROTO_CLASS_ID_ = ${context.PROTO_CLASS_ID};
    #end

    #if(${getterMethod})
        #foreach($field in ${context.fields})
            $!{field.comment}
            ${field.annotation}
            #if(${field.isList})
                List<${field.wrapperType.fullName}> ${field.getterMethodName}();
            #else
                ${field.wrapperType.fullName} ${field.getterMethodName}();
            #end
        #end
    #end

    #if(${setterMethod})
        #foreach($field in ${context.fields})
            $!{field.comment}
            void ${field.setterMethodName}(#if(${field.isList}) List<${field.wrapperType.fullName}> #else ${field.wrapperType.fullName} #end value);
        #end
    #end
}
