syntax = "${proto.version}"

option java_package = "${proto.package}"
option java_outer_classname = "${proto.classname}"

message ${proto.message} {

#foreach(${field} in ${proto.fields})
    #if(${field.type} == "double")
        double ${field.name} = ${velocityCount};
    #elseif(${field.type} == "float")
        float ${field.name} =  ${velocityCount};
    #elseif(${field.type} == "int")
        sint32 ${field.name} =  ${velocityCount};
    #elseif(${field.type} == "long")
        sint64 ${field.name} =  ${velocityCount};
    #elseif(${field.type} == "boolean")
        bool ${field.name} =  ${velocityCount};
    #elseif(${field.type} == "String")
        string ${field.name} =  ${velocityCount};
    #elseif(${field.type} == "ByteString")
        bytes ${field.name} =  ${velocityCount};
    #end

#end

}
