install:
  - wget https://github.com/google/protobuf/releases/download/v3.5.1/protobuf-all-3.5.1.tar.gz
  - tar -xzvf protobuf-all-3.5.1.tar.gz
  - cd protobuf-all-3.5.1 && ./autogen.sh && ./configure --prefix=/usr && make && sudo make instal

language: java

java:
   - oraclejdk8

